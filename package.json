{
  "name": "picsup",
  "scripts": {
    "dev:setup": "rimraf frontend/dist && pnpm -r install",
    "dev": "pnpm -r dev",
    "start": "pnpm -F frontend build && pnpm -F backend start"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
