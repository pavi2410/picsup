{
  "name": "picsup",
  "scripts": {
    "dev:setup": "rimraf frontend/dist && pnpm -r install",
    "dev": "pnpm -r dev",
    "build": "pnpm -F frontend build",
    "start": "pnpm -F backend start"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
