{
  "name": "picsup",
  "private": true,
  "scripts": {
    "build": "bun --filter=frontend run build",
    "start": "bun --filter=backend run start"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}